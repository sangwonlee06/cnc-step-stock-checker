services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "8080"
    expose:
      - "8080"

  frontend:
    build:
      context: .
      dockerfile: frontend-react/Dockerfile
    depends_on:
      - backend
    ports:
      - "8080:80"
